# Orchestration
langgraph>=0.2
langchain-core>=0.3
langchain-ollama>=0.2

# Search tool (§2.5 of the learning guide — free, no API key)
ddgs>=9.0

# Structured output / schemas
pydantic>=2.0

# Loads .env for any future API key without hardcoding (§3.8)
python-dotenv>=1.0

# Testing
pytest>=8.0
